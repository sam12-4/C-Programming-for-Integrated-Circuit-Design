#include <stdio.h>

int main(){
    int a;
    printf("The size of a is %d\n", sizeof(a) );
    char c = 'm';
    printf("The size of char is %c\n", sizeof(c) );
    float f  = 4.5;
    printf("The size of a is %d", sizeof(f) );
}