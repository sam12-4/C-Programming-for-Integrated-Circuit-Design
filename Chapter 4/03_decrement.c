#include <stdio.h>

int main(){
    int i=0;
    printf("The value of i is %d\n", i++); ///post increment operator increments after the statement is executed
    printf("The value of i is %d", i);
    return 0;
}