#include <stdio.h>

// calcualting area of circle and modifying it to calculate the volume of cyinder
int main(){
    int r = 3;
    int h = 4;
    printf("The area of circle is %f and the volume of cyliner is %f", 3.142*r*r, 3.142*r*r*h);
    return 0;
}